function checkSign(num) {
  return (num === 0) ? "zero"
  :(num > 0) ? "positive" 
  : "negative";
  }
  
  checkSign(10);

  // unsure about formating would like to pull the last condition up because it's complete