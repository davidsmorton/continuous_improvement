// The global variable
var fixedValue = 4;

function incrementer () {
  // Only change code below this line
let plusOne = fixedValue + 1; 
console.log(plusOne)
return plusOne

  // Only change code above this line
}

// bit a trouble sorting out += this seems to change the fixedValue but "1 + "